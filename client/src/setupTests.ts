/**
 * Konfigurace testovacího prostředí
 * Přidává custom jest matchers pro testování DOM elementů
 * Umožňuje použití funkcí jako expect(element).toHaveTextContent(/text/i)
 * Více informací: https://github.com/testing-library/jest-dom
 */
import '@testing-library/jest-dom';
